<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_helpdesk_stage_tree" model="ir.ui.view">
        <field name="name">helpdesk.stage.tree</field>
        <field name="model">helpdesk.stage</field>
        <field name="arch" type="xml">
            <list>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="is_start"/>
                <field name="is_done"/>
                <field name="team_ids" widget="many2many_tags"/>
            </list>
        </field>
    </record>

    <record id="view_helpdesk_stage_form" model="ir.ui.view">
        <field name="name">helpdesk.stage.form</field>
        <field name="model">helpdesk.stage</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="sequence"/>
                        </group>
                        <group>
                            <field name="is_start"/>
                            <field name="is_done"/>
                            <field name="fold"/>
                        </group>
                    </group>
                    <group>
                        <field name="team_ids" widget="many2many_tags"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_helpdesk_stage_tree" model="ir.actions.act_window">
        <field name="name">Stages</field>
        <field name="res_model">helpdesk.stage</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
