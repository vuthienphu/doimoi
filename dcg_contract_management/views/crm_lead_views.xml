<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- crm.lead form: add Contracts smart button to button_box
         (div[@name='button_box'] verified to exist in crm.crm_lead_view_form, Odoo 18) -->
    <record id="crm_lead_view_form_dcg_contract" model="ir.ui.view">
        <field name="name">crm.lead.form.dcg.contract</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_contracts" type="object"
                        class="oe_stat_button" icon="fa-file-text"
                        invisible="type != 'opportunity' or not contract_count">
                    <field name="contract_count" widget="statinfo" string="Contracts"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
