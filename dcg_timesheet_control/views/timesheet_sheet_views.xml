<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_dcg_timesheet_sheet_list" model="ir.ui.view">
        <field name="name">dcg.timesheet.sheet.list</field>
        <field name="model">dcg.timesheet.sheet</field>
        <field name="arch" type="xml">
            <list string="Timesheet Sheets" sample="1">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="date_start"/>
                <field name="date_end"/>
                <field name="period_type" optional="show"/>
                <field name="total_hours" sum="Total"/>
                <field name="total_billable_hours" sum="Total" optional="show"/>
                <field name="total_ot_hours" sum="Total" optional="show"/>
                <field name="approver_id" optional="show"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'approved'"
                       decoration-info="state == 'submitted'"
                       decoration-danger="state == 'rejected'"
                       decoration-muted="state == 'cancelled'"/>
            </list>
        </field>
    </record>

    <record id="view_dcg_timesheet_sheet_form" model="ir.ui.view">
        <field name="name">dcg.timesheet.sheet.form</field>
        <field name="model">dcg.timesheet.sheet</field>
        <field name="arch" type="xml">
            <form string="Timesheet Sheet">
                <header>
                    <button name="action_load_lines" type="object" string="Load Lines"
                            invisible="state != 'draft'"/>
                    <button name="action_submit" type="object" string="Submit"
                            class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" type="object" string="Approve"
                            class="btn-primary" invisible="state != 'submitted'"/>
                    <button name="action_reject" type="object" string="Reject"
                            invisible="state != 'submitted'"/>
                    <button name="action_reset_to_draft" type="object" string="Reset to Draft"
                            invisible="state not in ('submitted', 'rejected', 'approved')"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state in ('approved', 'cancelled')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group string="Employee">
                            <field name="employee_id"/>
                            <field name="user_id" readonly="1"/>
                            <field name="approver_id"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                        <group string="Period">
                            <field name="period_type"/>
                            <field name="date_start"/>
                            <field name="date_end"/>
                            <field name="week_no" readonly="1"/>
                            <field name="month_key" readonly="1"/>
                        </group>
                    </group>
                    <group string="Summary">
                        <group>
                            <field name="total_hours" readonly="1"/>
                            <field name="total_billable_hours" readonly="1"/>
                        </group>
                        <group>
                            <field name="total_non_billable_hours" readonly="1"/>
                            <field name="total_ot_hours" readonly="1"/>
                            <field name="line_count" readonly="1"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Timesheet Lines" name="lines">
                            <field name="line_ids"
                                   readonly="state in ('approved', 'cancelled')">
                                <list editable="bottom">
                                    <field name="date"/>
                                    <field name="dcg_project_id"/>
                                    <field name="dcg_scope_id"/>
                                    <field name="name" string="Description"/>
                                    <field name="unit_amount" string="Hours" sum="Total"/>
                                    <field name="charge_type"/>
                                    <field name="work_type" optional="show"/>
                                    <field name="is_overtime" widget="boolean_toggle" optional="show"/>
                                    <field name="overtime_hours" optional="hide" sum="OT"/>
                                    <field name="line_state" widget="badge" readonly="1" optional="show"/>
                                </list>
                            </field>
                        </page>
                        <page string="Approval" name="approval"
                              invisible="state == 'draft'">
                            <group>
                                <field name="submitted_date" readonly="1"/>
                                <field name="approved_date" readonly="1"/>
                                <field name="rejected_date" readonly="1"/>
                                <field name="reject_reason"/>
                            </group>
                        </page>
                        <page string="Notes" name="notes">
                            <field name="note"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_dcg_timesheet_sheet_search" model="ir.ui.view">
        <field name="name">dcg.timesheet.sheet.search</field>
        <field name="model">dcg.timesheet.sheet</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="employee_id"/>
                <field name="approver_id"/>
                <separator/>
                <filter string="My Sheets" name="filter_my"
                        domain="[('user_id', '=', uid)]"/>
                <filter string="To Approve" name="filter_to_approve"
                        domain="[('approver_id', '=', uid), ('state', '=', 'submitted')]"/>
                <separator/>
                <filter string="Draft" name="filter_draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Submitted" name="filter_submitted" domain="[('state', '=', 'submitted')]"/>
                <filter string="Approved" name="filter_approved" domain="[('state', '=', 'approved')]"/>
                <filter string="Rejected" name="filter_rejected" domain="[('state', '=', 'rejected')]"/>
                <group expand="0" string="Group By">
                    <filter string="Employee" name="group_employee" context="{'group_by': 'employee_id'}"/>
                    <filter string="Approver" name="group_approver" context="{'group_by': 'approver_id'}"/>
                    <filter string="Month" name="group_month" context="{'group_by': 'month_key'}"/>
                    <filter string="State" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_dcg_timesheet_sheet" model="ir.actions.act_window">
        <field name="name">Timesheet Sheets</field>
        <field name="res_model">dcg.timesheet.sheet</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_filter_my': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create or generate a timesheet sheet</p>
        </field>
    </record>

    <record id="action_dcg_timesheet_sheet_to_approve" model="ir.actions.act_window">
        <field name="name">Sheets to Approve</field>
        <field name="res_model">dcg.timesheet.sheet</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_filter_to_approve': 1}</field>
    </record>

</odoo>
