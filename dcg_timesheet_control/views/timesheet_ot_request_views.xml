<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_dcg_timesheet_ot_request_list" model="ir.ui.view">
        <field name="name">dcg.timesheet.ot.request.list</field>
        <field name="model">dcg.timesheet.ot.request</field>
        <field name="arch" type="xml">
            <list string="OT Requests">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="request_date"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="total_hours" sum="Total OT"/>
                <field name="project_id" optional="show"/>
                <field name="approver_id" optional="show"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'approved'"
                       decoration-info="state == 'submitted'"
                       decoration-danger="state == 'rejected'"
                       decoration-muted="state == 'cancelled'"/>
            </list>
        </field>
    </record>

    <record id="view_dcg_timesheet_ot_request_form" model="ir.ui.view">
        <field name="name">dcg.timesheet.ot.request.form</field>
        <field name="model">dcg.timesheet.ot.request</field>
        <field name="arch" type="xml">
            <form string="OT Request">
                <header>
                    <button name="action_submit" type="object" string="Submit"
                            class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" type="object" string="Approve"
                            class="btn-primary" invisible="state != 'submitted'"/>
                    <button name="action_reject" type="object" string="Reject"
                            invisible="state != 'submitted'"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state in ('approved', 'cancelled')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="employee_id"/>
                            <field name="user_id" readonly="1"/>
                            <field name="request_date"/>
                            <field name="approver_id"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                        <group>
                            <field name="date_from"/>
                            <field name="date_to"/>
                            <field name="total_hours" readonly="1"/>
                            <field name="project_id"/>
                            <field name="scope_id"/>
                        </group>
                    </group>
                    <group string="Reason">
                        <field name="reason" nolabel="1"/>
                    </group>
                    <group string="Approval" invisible="state == 'draft'">
                        <field name="approved_date" readonly="1"/>
                        <field name="reject_reason"/>
                    </group>
                    <notebook>
                        <page string="Linked Timesheet Lines" name="lines">
                            <field name="timesheet_line_ids">
                                <list create="false">
                                    <field name="date"/>
                                    <field name="dcg_project_id"/>
                                    <field name="name"/>
                                    <field name="unit_amount"/>
                                    <field name="overtime_hours"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_dcg_timesheet_ot_request_search" model="ir.ui.view">
        <field name="name">dcg.timesheet.ot.request.search</field>
        <field name="model">dcg.timesheet.ot.request</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="employee_id"/>
                <field name="project_id"/>
                <filter string="My OT" name="filter_my" domain="[('user_id', '=', uid)]"/>
                <filter string="To Approve" name="filter_to_approve"
                        domain="[('approver_id', '=', uid), ('state', '=', 'submitted')]"/>
                <filter string="Approved" name="filter_approved" domain="[('state', '=', 'approved')]"/>
                <group expand="0" string="Group By">
                    <filter string="Employee" name="group_employee" context="{'group_by': 'employee_id'}"/>
                    <filter string="Project" name="group_project" context="{'group_by': 'project_id'}"/>
                    <filter string="State" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_dcg_timesheet_ot_request" model="ir.actions.act_window">
        <field name="name">OT Requests</field>
        <field name="res_model">dcg.timesheet.ot.request</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_filter_my': 1}</field>
    </record>

</odoo>
